Need to create the .env to run the mess

> .env

- WORKDIR_PATH=/1/snake
- PORT=5001
- GAME_PATH=/Game-Vault/snake.py

> build docker

    docker-compose build

    docker-compose up

> macos

    source venv/bin/activate

    pip install -r watchdog

    python mon_script.py
